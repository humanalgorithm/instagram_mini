
<% if user_signed_in? %>
  <div class="margin-top-40">
    <h2>Logged in as: <%= current_user.email%> </h2>
	  <%= link_to "Upload a new image", new_image_path(:user_id => current_user.id)%>
	<br>
      <%= link_to "See your images", view_images_path(:user_id => current_user.id) %>
  </div>
<% else %>
	<div class="margin-top-40">
		<h2> Welcome to the mini instagram page</h2>
	</div>
<% end %>
